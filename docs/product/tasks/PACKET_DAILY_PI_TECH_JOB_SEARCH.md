# Daily PI technology career search

## Workdir + Source
/home/pranav/possibleos. User explicitly asks: "Need this job search done every day, with new jobs entered in possibleos job postings, how to do this ?" Standing authorization: parent orchestrates existing pinned tasks; Possible OS owns platform implementation. Implement this bounded workflow now. Read AGENTS.md/CLAUDE.md and preserve current WIP (snapshot /tmp/possibleos-before-daily-career-search.patch). Prior search report /home/pranav/resume/job-search/2026-09-10-pi-tech-remote-jobs.md and JSON provide seed evidence.

## Scope
Inspect your latest task changes and current jobs maintenance first; avoid duplicating work. Existing pif research maintenance revisits firms every7/30days (4510firms,175daily cap). User needs a SEPARATE targeted daily career search that discovers new PI firms as well as rechecks known careers sources. Existing MissionControl mc-jobs-codex is unrelated India search: do not alter.

Build the smallest maintainable daily workflow using existing Possible OS jobs store/upsert/classification and server-side runtime. Local research via working OpenClaw/main, or existing tested server runtime; NEVER EmailTag web research. Native automation_update capability was searched and unavailable. Prefer reuse existing scheduling infrastructure if appropriate; otherwise prepare systemd units in repo deploy only for parent installation. Default daily08:00 America/Bogota (13:00UTC); parent asked user optional time preference, may steer answer. Do not await time answer to build reusable config.

Daily query target: direct personal-injury law firms, especially technology/data, software engineering, workflow/process automation, legal AI agents, voice/intake platforms. Prioritize remote and Colombia/LATAM accessible; retain US-restricted remote with correct label, not globally eligible. Exclude ordinary attorneys/paralegals/case managers merely using software; separate unnamed recruiters/vendors if recorded. Query rolling30days, retain original employer posted date separately from first_seen and ATS created/updated dates. Verify live employer/ATS job page before active ingest; redirects to generic boards,410/404,closed forms and misattribution are not active jobs. Date-unknown leads labeled, no invented dates.

Must write NEW verified results into existing Possible OS Job Postings UI/store, canonical firm matching and source/requisition dedup; merge not replace existing unrelated jobs/firm research. Newly discovered verified PI firms can be minimally upserted via canonical existing firm services. Reruns idempotent, lock/retry/backoff and durable run counts/status/errors. Preserve original posted/first_seen and latest checked time. Recheck tracked links for closed status without treating transient errors as closure. Bound research work per run and avoid full4510firm daily LLM crawl. Use current five priority jobs as initial end-to-end ingest verification if not already stored; do not create fakes.

Expose CLI run/status/configuration controls + docs as required, meaningful tests for dedup/merge/date and geography evidence, and one bounded real discovery+ingest run after parent deploy (or using existing CLI/service if safely available). Do not silently claim success if LLM gateway or import fails. Save last-run counters and error details in inspectable durable state. Keep quiet for zero-new successful runs, surface new matches/failures in existing UI/status; NO unsolicited emails or external messages.

## Guardrails
No applications/resumes/emails/outreach. No changes to unrelated outreach scheduler, call dispatcher, existing India jobs schedule, or bulkbackfills. No blind all-firm daily scheduling. No commits/pushes/service restarts, /etc writes or systemd installs by implementer: parent handles necessary deployment after audit. Reuse your owning-task context. Preserve other tracked/untracked work; no reverting broad hunks. Avoid frontend overhaul; jobs should appear in existing job-postings view. No new Codex tasks. Report architecture findings early, before lengthy implementation.

## Validation / report
Run focused relevant pytest cases (existing test_pif_job_posting_research, test_cli_pif_job_postings and new meaningful coverage); exact commands selected from current repo environment. Report files changed, backend/CLI behavior, tests, install/restart/migration instructions, and known limits. Include schedule timezone, next run, idempotent ingest counts and example stored IDs once possible. Parent will independently review and live-enable then verify persistence; do not call a staged unit an active schedule. Update docs/cli.md, required skills mirror, relevant feature doc and append decision log for architecture choices. If prompt edits require project prompt-change protocol, identify precisely so parent completes it.
