# 2026-07-15

## Lead-gen deliverability diagnostic rails

- **Decision:** Treat a recipient email domain and PIF firm record as touched after any prior email log, lead-gen batch item, or outreach send. Curated selection now defaults to domain-level and firm-level freshness with one contact per domain and firm, while explicit flags retain contact-only freshness and higher caps for intentional follow-ups.
- **Why:** The July 13-14 Filevine waves reached multiple contacts at duplicate firm records and shared domains. Provider delivery was high, but human engagement was unproven and the bounce rate was elevated. Domain-level rails reduce complaint and bulk-classification risk before testing new copy.
- **Decision:** Count terminal Resend webhook states such as delivered, bounced, complained, opened, and clicked toward the daily provider cap.
- **Why:** A webhook transition must not remove a send from the daily counter and accidentally restore capacity.
- **Decision:** The next diagnostic wave is reply-only, contains no workshop or tracking link, uses a visible opt-out sentence, and remains unscheduled until the sender identity is explicitly selected.

## Filevine case-manager reply-only launch

- **Status:** Active; supersedes the earlier scheduling deferral after the operator explicitly selected the Resend path and approved same-day scheduling.
- **Decision:** Use the manually written pre-demand checklist offer with subject `quick Filevine question`, no link or attachment, and a yes/no reply ask. Do not use the composer.
- **Why:** A concrete, role-relevant artifact gives recipients a low-effort reason to reply while preserving the no-link human-reach diagnostic.
- **Decision:** Send the full 20-recipient cohort through Resend on July 15, four minutes apart, with Zoho disabled and both the Resend cap and total daily budget set to 20.
- **Why:** Keeping all recipients in one same-day window avoids a day-of-send confound while remaining at the system's prior daily volume.
- **Decision:** Temporarily disable the persisted automatic daily lead-gen run while this 20-message diagnostic is active; re-enable it after the experiment closes.
- **Why:** The independent daily loop still had a 20-message target and could create a second cohort, contaminating the controlled window. This flag does not cancel the explicitly approved scheduled actions.

## D-2026-07-15-01 — Keep the returned-data shell script fixed and read-only
- **Area:** infra
- **Status:** accepted
- **Decision:** Expose `GET /datareturned/script` as a public fixed diagnostic script whose final command posts combined JSON output to `/datareturned`. Do not accept arbitrary commands, callback URLs, or environment/file collection.
- **Why:** Remote callback verification needs a zero-configuration script, while a programmable public shell-script generator or broad environment dump would create an unnecessary execution and secret-exfiltration surface.
- **Refs:** `app/api/data_returned.py`, `app/services/data_returned.py`, `/data-returned`
